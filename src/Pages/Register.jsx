import { Helmet } from "react-helmet";

const Register = () => {
    return (
        <div>
            <Helmet>
                <title>FeedX | Register</title>
            </Helmet>
            <p>Register</p>
        </div>
    );
};

export default Register;