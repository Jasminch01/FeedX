import { Helmet } from "react-helmet";

const AddFoods = () => {
    return (
        <div>
            <Helmet>
                <title>FeedX | add food</title>
            </Helmet>
            <p>Add Food</p>
        </div>
    );
};

export default AddFoods;