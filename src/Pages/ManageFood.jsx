import { Helmet } from "react-helmet";

const ManageFood = () => {
    return (
        <div>
            <Helmet>
                <title>FeedX | Manage Food</title>
            </Helmet>
            <p>Manage Food</p>
        </div>
    );
};

export default ManageFood;